mudei o final do arquivo main.py para deploy no render
 estava assim: uvicorn.run("main:app", host="0.0.0.0", port=8000, reload=True)

 Mudei para:   if __name__ == "__main__":
       import uvicorn
       port = int(os.environ.get("PORT", 8000))
       uvicorn.run("main:app", host="0.0.0.0", port=port)